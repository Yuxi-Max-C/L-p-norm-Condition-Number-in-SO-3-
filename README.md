# SO(3) Condition Number Dataset Generator

This repository provides a Python script to generate a dataset of condition numbers for a tetrahedral measurement frame under rotations in SO(3).

---

## 📦 Requirements

Install required packages:

```bash
pip install numpy tqdm
